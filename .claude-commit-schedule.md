# Git Commit Schedule (DO NOT COMMIT THIS FILE)

**Strategy:** Historical backfill - one commit per day showing milestone progression

**Important:** Commits represent historical development stages, NOT current work.
We can work ahead on M4/M7/etc. while commits catch up to show M1 → M2 → M3 progression.

## Completed Commits

### ✅ Commit 1 (Completed) - M6 Search Foundation
**Date:** January 17, 2026
**Message:** `feat(search): Add search filter system and store foundation`

**Files:**
- `apps/web/lib/search/filters.ts`
- `apps/web/stores/search-store.ts`

---

## Scheduled Commits (Historical Backfill)

### 📅 Commit 2 (Tomorrow - Day 2)
**Message:** `feat(search): Implement SearchPanel UI with Obsidian-inspired features`

**Files to stage:**
```bash
git add apps/web/components/notes/SearchPanel.tsx
```

**Commit command:**
```bash
git commit -m "feat(search): Implement SearchPanel UI with Obsidian-inspired features

- Create full-featured SearchPanel component with debounced search
- Add match case toggle (Aa) with gold active state
- Add regex search toggle (.*) with error handling
- Add document type filter dropdown (all, note, folder, html, code, file)
- Add 'More context' checkbox for dynamic excerpt length
- Add 'Auto-scroll to match' checkbox with URL parameter preparation
- Add clear button (X) that appears when text is present
- Implement keyboard navigation (Arrow Up/Down, Enter)
- Add empty states for no query and no results
- Add loading state with spinner
- Use white background (bg-white/90) with dark text for readability
- Dynamic line-clamp: 4 lines with context, 2 lines without
- Click result to open content via content store integration
- Results show title, type badge, excerpt with highlighting, path, date, match count

Part of M6 Search & Knowledge Features - Phase 1

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

---

### 📅 Commit 3 (Day 3)
**Message:** `feat(search): Integrate search with file tree navigation`

**Files to stage:**
```bash
git add apps/web/components/notes/headers/LeftSidebarHeader.tsx \
         apps/web/components/notes/headers/LeftSidebarHeaderActions.tsx \
         apps/web/components/notes/content/LeftSidebarContent.tsx \
         apps/web/components/notes/ResizablePanels.tsx
```

**Commit command:**
```bash
git commit -m "feat(search): Integrate search with file tree navigation

- Update LeftSidebarHeader with tab-style icon navigation
- Show both Files and Search icons at all times
- Active icon gets gold color, inactive is gray with hover
- Add Cmd+/ keyboard shortcut to toggle search
- Update LeftSidebarContent to conditionally render FileTree or SearchPanel
- Integrate search store with panel layout
- Clicking Files icon closes search and returns to file tree
- Clicking Search icon opens search panel

Part of M6 Search & Knowledge Features - Phase 1

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

---

### 📅 Commit 4 (Day 4)
**Message:** `feat(search): Add search highlighting and API integration`

**Files to stage:**
```bash
git add apps/web/app/api/notes/content/route.ts \
         apps/web/app/globals.css
```

**Note:** For globals.css, only include the search match highlighting section (lines 600-607), NOT the callout styling (lines 609-752). The callout CSS will be committed later with M7.

**Commit command:**
```bash
git commit -m "feat(search): Add search highlighting and API integration

- Update GET /api/notes/content to include searchText in payload responses
- Add searchText to notePayload, htmlPayload, and codePayload selectors
- Add <mark> tag styling with gold theme for search match highlighting
- Gold background (rgba 255,215,0 at 30% opacity)
- Gold text color (#FFD700) with 600 font-weight
- 2px padding and border-radius for smooth appearance
- API now provides full-text search data for excerpt generation

Part of M6 Search & Knowledge Features - Phase 1

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

**IMPORTANT:** Before this commit, temporarily remove the callout CSS from globals.css (lines 609-752) and restore it after committing.

---

### 📅 Commit 5 (Day 5)
**Message:** `docs(search): Add M6 search implementation documentation`

**Files to create & stage:**
- `apps/web/docs/notes-feature/M6-SEARCH-IMPLEMENTATION.md`

**Commit command:**
```bash
git commit -m "docs(search): Add M6 search implementation documentation

- Document M6 Search & Knowledge Features - Phase 1 completion
- Describe modular search filter system architecture
- Document localStorage persistence strategy
- List all Obsidian-inspired features implemented
- Document API integration and searchText field usage
- Provide component architecture overview
- Add troubleshooting section for common issues
- Document keyboard shortcuts (Cmd+/, Arrow keys, Enter)
- List pending Phase 2 features (backlinks, outline, wiki links)

Part of M6 Search & Knowledge Features - Phase 1

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

---

## After M6 Search Complete (Day 6+)

### Next: M4 File Tree Completion (~60% remaining)
- Context menu (right-click)
- Keyboard navigation
- Inline rename
- Multi-selection
- Keyboard shortcuts
- Optimistic updates polish
- Tree state persistence

### Then: M7 Callouts
- Fix callout-extension.ts input rules
- Test Obsidian syntax (`> [!note]`)
- Test slash commands (`/callout note`)
- Add title editing functionality
- Commit callout files:
  - `lib/editor/callout-extension.ts`
  - `lib/editor/extensions.ts` (callout integration)
  - `lib/editor/slash-commands.tsx` (callout commands)
  - `app/globals.css` (callout styling lines 609-752)

---

## Notes

- **DO NOT COMMIT** this tracking file (`.claude-commit-schedule.md`)
- Add to `.gitignore` if needed: `.claude-commit-schedule.md`
- One commit per day strategy for smooth public progress
- Each commit should be complete and functional
- Test each commit before executing
- M7 callout files are already created but not committed yet
