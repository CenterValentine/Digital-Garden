/**
 * STUB — full-text search across notes.
 *
 * Scope:
 *   - Cmd+/ opens search panel
 *   - Typing in search input filters results live
 *   - Result click navigates to the matching note
 *   - Empty query shows recent notes / suggestions
 *   - Filter chips (tags, content types) narrow results
 */

import { test } from "@playwright/test";

test.describe("search: full-text", () => {
  test.skip("Cmd+/ opens search panel", async ({ page }) => {
    void page;
  });

  test.skip("results filter live on keystroke", async ({ page }) => {
    void page;
  });

  test.skip("result click navigates to note", async ({ page }) => {
    void page;
  });

  test.skip("filter chips narrow results", async ({ page }) => {
    void page;
  });
});
