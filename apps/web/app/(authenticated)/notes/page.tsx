/**
 * Notes Page - Main entry point
 *
 * Renders the main panel with editor/viewer.
 */

import { MainPanel } from "@/components/notes/MainPanel";

export default function NotesPage() {
  return <MainPanel />;
}
