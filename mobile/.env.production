# Inlined into the JS bundle by Expo's export step for Release builds —
# including builds driven from the Xcode GUI, where shell env vars don't
# reach the bundling phase. EXPO_PUBLIC_* values are public by definition;
# committing this makes production builds reproducible.
EXPO_PUBLIC_DIGITAL_GARDEN_URL=https://davidvalentine.org/mobile
