import { InvitePage } from "@/components/personal/InvitePage";

export const metadata = {
  title: "Start a Digital Garden — Note Trellis",
  description:
    "Note Trellis is a knowledge tool for people who think in connections. Access is by invitation — leave your email and we'll be in touch.",
};

export default function Invite() {
  return <InvitePage />;
}
