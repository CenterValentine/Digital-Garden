{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": { "cache": false, "persistent": true },
    "build": { "dependsOn": ["^build"], "outputs": ["apps/*/.next/**", "apps/*/dist/**"] },
    "typecheck": { "outputs": [] },
    "lint": { "outputs": [] }
  }
}
